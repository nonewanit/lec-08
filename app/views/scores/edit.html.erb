<h1>Editing score</h1>

<%= render "form", score: @score %>

<br>

<div>
  <% if session[:from]=="score_index" %>
    <div>student id = <%= @score.student.id%></div>
  <% end %>
  <%= link_to "Show this score", @score %> |
  <%= link_to "Back to scores", scores_path %>
</div>
